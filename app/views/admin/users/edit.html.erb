<%= form_for [:admin, @user], action: :edit do |f| %>

  <%= f.label :admin %>
  <%= f.check_box(:admin) %>

  <%= f.label :courses_limit %>
  <%= f.text_field :courses_limit %>

  <%= f.submit %>

<% end %>
